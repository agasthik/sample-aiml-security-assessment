# FinServ Security Assessment Lambda — runtime dependencies
#
# These deps are scanned by ASH (Automated Security Helper) via Grype + Syft
# during the pre-PR security scan. boto3/botocore are pinned to the repository's
# tested SDK contract for reproducible Lambda builds. The pins must remain at or
# above the minimum required by FS-03/FS-06 (see TestRequirementVersionPins).
# If ASH reports a CVE, update both pins repository-wide and rerun the SDK
# contract and assessment test suites before opening the PR.
#
# Dev tooling (ruff, cfn-lint, ash, sam) is NOT pinned here — install those
# in your development environment, not into the Lambda image.

boto3==1.43.85
botocore==1.43.85
pydantic>=2.0.0,<3.0.0
