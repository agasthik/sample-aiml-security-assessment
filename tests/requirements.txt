pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0
moto[all]>=4.2.0
boto3==1.43.85
botocore==1.43.85
# Upper bound matches the Lambda runtime requirement
# (responsible_ai_grc_assessments/requirements.txt), so tests cannot pass
# against a pydantic major the deployed function forbids.
pydantic>=2.0.0,<3.0.0
beautifulsoup4==4.13.4
reportlab==5.0.0
ripgrep>=15.1.0
# Imported directly by tests/test_legacy_contracts.py to parse the SAM and
# deployment CloudFormation templates. Available transitively via moto[all],
# but declared explicitly so the contract-lock gate cannot break if that
# transitive dependency changes.
pyyaml>=6.0
